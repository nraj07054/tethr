# ── macOS ──────────────────────────────────────────────
.DS_Store

# ── Mac app · Swift Package Manager (mac/) ─────────────
mac/.build/
.swiftpm/
# The built app binary, its copied resource bundle, and the signature are
# outputs; Info.plist and AppIcon.icns are kept so the .app can be reproduced.
mac/Tethr.app/Contents/MacOS/
mac/Tethr.app/Contents/_CodeSignature/
mac/Tethr.app/Contents/Resources/Tethr_Tethr.bundle/

# ── Android app · Gradle (android/) ────────────────────
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.iml
*.hprof

# ── Misc ───────────────────────────────────────────────
*.log
